# 20260215-122416 CI修复：改用官方 Node 22 二进制

## 背景
- r3 构建仍失败，推断与 Node 版本/包源兼容链路有关（opencode-manager 构建依赖较新 Node）。

## 调整
- Dockerfile 新增 ARG NODE_VERSION=22.14.0。
- 移除 apt 安装 
odejs/npm。
- 改为下载并解压官方 Node 22 二进制到 /usr/local。
- 保留 corepack + pnpm@10.28.1 作为 manager 构建工具链。

## 目的
- 固定 Node 版本并消除 apt / NodeSource 差异导致的 CI 不确定性。
