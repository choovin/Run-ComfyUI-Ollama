# CI修复：bun 安装缺少 unzip

## 现象
- GitHub Actions 构建失败，日志报错：error: unzip is required to install bun。

## 根因
- Dockerfile 依赖安装中未包含 unzip。
- bun 安装脚本需要 unzip 才能解压安装包。

## 修复
- 在 Dockerfile 的 apt 安装列表中新增 unzip。

## 后续
- 重新触发 workflow（推荐打新 tag 或 workflow_dispatch）验证构建。
